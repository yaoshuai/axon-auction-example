<?xml version="1.0" encoding="UTF-8"?>
<dependencies>
  
  <!-- All classes are allowed to use this -->
  <alwaysAllowed>
    <dependsOn package="java.lang"/>
    <dependsOn package="java.io"/>
    <dependsOn package="java.util"/>
    <dependsOn package="java.text"/>
  </alwaysAllowed>
  
  <!-- It's forbidden for all classes to use this (in production code) -->
  <alwaysForbidden>
    <notDependsOn package="junit.framework"/>
    <notDependsOn package="org.junit"/>
  </alwaysForbidden>
  
  <!-- Packages are ONLY allowed to use the following packages (all other packages are forbidden) -->
  <allowed/>

  <!-- Packages are NOT allowed to use the following ones (all other packages are allowed) -->  
  <forbidden>
    <package name="org.fuin.auction.common" comment="Common is not allowed to access any of the other projects">
      <notDependsOn package="org.fuin.auction.command"/>
      <notDependsOn package="org.fuin.auction.query"/>    
      <notDependsOn package="org.fuin.auction.message"/>
      <notDependsOn package="org.fuin.auction.client"/>
    </package>
  </forbidden>
  
</dependencies>
